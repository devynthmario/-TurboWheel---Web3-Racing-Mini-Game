{
  "name": "turbowheel",
  "version": "1.0.0",
  "description": "Web3 racing mini-game with Yellow Nitrolite SDK integration",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install && cd ../contracts && npm install"
  },
  "keywords": ["web3", "racing", "game", "ethereum", "yellow-sdk", "erc-7824"],
  "author": "TurboWheel Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
